# Frontend/Backend
- Properly implement Pagination for Reviews in ReviewList.tsx. API already supports pagination for reivews but i forgot to return the total count.

- Check if the user has already reviewed the current game to switch (create review) to (edit review). ()